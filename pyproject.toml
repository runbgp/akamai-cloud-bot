[project]
name = "akamai-cloud-bot"
version = "0.1.0"
description = "A Discord bot that manages Akamai Cloud instances"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "discord.py==2.7.1",
    "python-dotenv==1.2.3",
    "requests==2.34.2",
]
